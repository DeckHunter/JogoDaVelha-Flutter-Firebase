import 'package:flutter/cupertino.dart';

class VitoriaX extends StatelessWidget {
  const VitoriaX({Key? key}) : super(key: key);

  @override
  // ignore: override_on_non_overriding_member
  Widget build(BuildContext context) {
    return Container();
  }
}

class VitoriaO extends StatelessWidget {
  const VitoriaO({Key? key}) : super(key: key);

  @override
  // ignore: override_on_non_overriding_member
  Widget build(BuildContext context) {
    return Container();
  }
}

class Empate extends StatelessWidget {
  const Empate({Key? key}) : super(key: key);

  @override
  // ignore: override_on_non_overriding_member
  Widget build(BuildContext context) {
    return Container();
  }
}
